import React from 'react';

let Spinner = (props) => {

    return(

      <a id='repeatButton' onClick={props.onClick} className="f6 link dim ph3 pv2 mb2 dib white bg-dark-blue" href="#0">Button Text</a>
        
    )
}

export default Spinner;